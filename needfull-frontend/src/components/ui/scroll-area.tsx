import * as React from "react"

export const ScrollArea = ({ children, className }: any) => <div className={className}>{children}</div>
export const ScrollBar = ({ orientation, className }: any) => <div className={className}></div>
